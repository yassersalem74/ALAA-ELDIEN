import ProfileSectionPlaceholder from "../ProfileSectionPlaceholder";

export default function ProfileCartSection() {
  return (
    <ProfileSectionPlaceholder
      title="Cart"
      description="This cart page is ready for saved items, service packages, and checkout details."
    />
  );
}
